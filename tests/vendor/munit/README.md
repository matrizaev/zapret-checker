# µnit

The files `munit.c`, `munit.h`, and `COPYING` are copied without modification
from [nemequ/munit](https://github.com/nemequ/munit) at commit
`fbbdf1467eb0d04a6ee465def2e529e4c87f2118`.

µnit is used only by the test binaries and is distributed under the MIT
license in `COPYING`.
